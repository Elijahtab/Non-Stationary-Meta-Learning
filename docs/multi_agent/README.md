# Multi-Agent / Ops Notes

Short operational plans for work that spans the **local machine + a rented cloud (Vast) box**,
where a separate agent/session may pick up the task. Distinct from `docs/handoffs/` (one-shot
"here's the state of a finished experiment") — these are *forward-looking* plans about the
infra/data pipeline itself.

## Index

- [0001 — Reduce artifacts created on the GPU box + auto send-back](./0001-reduce-gpu-artifacts.md)
</content>
